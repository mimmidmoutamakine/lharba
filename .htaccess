<IfModule mod_rewrite.c>
    RewriteEngine On

    # Send all requests through Laravel's public entrypoint on shared hosting.
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
